# User_Authentiction_API

A secure User Authentication API build using the Express, JWT, pnpm, MongoDB. Supports user registration, login, protected profile routes

auth-backend
│── package.json
│── pnpm-lock.yaml
│── .env
│── server.js
│
├── config
│   └── db.js
│
├── middleware
│   └── auth.js
│
├── models
│   └── User.js
│
├── routes
│   └── authRoutes.js
│
└── controllers
    └── authController.js
